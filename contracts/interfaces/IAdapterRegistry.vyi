"""
@title IAdapterRegistry
@author Curve Finance
@license MIT
@notice Read interface of the settlement adapter catalog used by the router.
@dev The struct is the single-source
     contracts/burners/auction/adapters/adapter_types.vy AdapterConfig. The
     verifier address is the registry lookup key, the adapter's identity, and
     the routing prefix of adapter signatures; the executor is the protocol
     contract auctions approve while an enabled adapter references it. An
     unknown verifier returns the zeroed config (executor == empty(address)).
"""

from contracts.burners.auction.adapters import adapter_types


@external
@view
def get_adapter(_verifier: address) -> adapter_types.AdapterConfig:
    ...
